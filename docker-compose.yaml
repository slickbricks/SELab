version: "3"
services:
  tljh:
    build: .
    privileged: true
    # restart: always
    ports:
      - 8889:80
    environment:
      TZ: UTC/NEW_YORK
    volumes:
      - ./config.yaml:/opt/tljh/config/config.yaml
